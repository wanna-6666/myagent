# 对话记录：从项目设计到实现

> 记录时间：2026-06-17
> 范围：从总结文档最后一轮更新到项目完整实现

---

## 一、简历内容定稿

生成了 5 条高吸引力简历描述（按优先级排序）：

1. **Agent 核心架构** — ReAct + Plan-and-Execute 双模式 + 三重循环保护
2. **工具并行调度** — 线程池并发执行 + Future + invokeAll + 按序回灌
3. **RAG 代码检索** — JavaParser AST 三级切块 + 混合检索 + RRF 融合
4. **SSE 流式 + 多模型适配** — 模板方法 + 工厂模式 + 热切换
5. **上下文工程 + Prompt Cache** — Map-Reduce 压缩 + 双作用域记忆 + 缓存策略

使用建议：空间够放 5 条，只放 4 条砍第 5 条，只放 3 条留 ①②③。

已同步到 `精简版Agent设计总结.md` 第五章。

---

## 二、更正：API Key 来源

更正 Q34 的回答：从"公司内部服务"改为"自己购买的 DeepSeek API，按量付费"。
Q40 补充："开发整个项目下来大概花了二三十块钱"。

---

## 三、功能取舍讨论

### 确认不实现的功能

| 功能 | 决定 | 理由 |
|------|------|------|
| **MCP 协议** | ❌ 不实现，能讲 | 太重，面试讲清楚架构设计即可 |
| **多模型热切换** | ⭐ 后实现 | 面试价值低（就是策略模式），但实际有用 |
| **TUI 界面** | ❌ | 展示层，不影响 Agent 核心 |
| **LSP 诊断** | ❌ | 太垂直，面试官不太会问 |
| **Git 快照** | ❌ 但能讲 | 概念简单有价值，面试一句话带过 |
| **Runtime API** | ❌ | 基础设施层，和 Agent 核心无关 |
| **HITL 审批** | ❌ 不实现，设计入文档 | 三层安全体系面试能讲 |
| **联网搜索** | ❌ 不实现，能讲 | 不带来新面试考点 |
| **Skill 插件** | ❌ 不实现，设计入文档 | 和工具注册表设计思路类似 |

### HITL 设计追加到文档

新增第 10 节：HITL 人机审批（设计说明），包含：
- ApprovalPolicy / ApprovalResult / HitlHandler / TerminalHitlHandler
- HitlToolRegistry 包装原始 ToolRegistry 拦截危险工具
- AuditLog 审计链
- CommandGuard + PathGuard 静态安全检查
- 安全体系三层防护图
- 面试话术

### Skill 插件设计追加到文档

新增第 11 节：Skill 插件系统（设计说明），包含：
- SKILL.md 文件格式（YAML frontmatter + Markdown 正文）
- 三层加载优先级（内置 < 用户 < 项目）
- SkillFrontmatterParser / SkillRegistry / SkillContextBuffer
- 面试话术

---

## 四、面试模拟 Q&A

生成了 42 个面试问题及完美答案，覆盖 11 个方向：

| 方向 | 题号 | 题数 |
|------|------|------|
| A. 项目整体架构 | Q1-Q4 | 4 |
| B. 设计模式 | Q5-Q7 | 3 |
| C. 并发编程 | Q8-Q10 | 3 |
| D. LLM / SSE 流式 | Q11-Q13 | 3 |
| E. 记忆系统 | Q14-Q16 | 3 |
| F. RAG | Q17-Q21 | 5 |
| G. Token 预算 | Q22-Q23 | 2 |
| H. 安全 | Q24-Q25 | 2 |
| I. 工程实践与 Trade-off | Q26-Q29 | 4 |
| J. 压力面 / 深追 | Q30-Q33 | 4 |
| K. 大模型接入 | Q34-Q42 | 9 |

已追加到 `精简版Agent设计总结.md` 第七章。

---

## 五、Vibe Coding 可信度确认

最终实现范围：
- 9 个模块要实现（ReAct / Plan-and-Execute / 工具系统 / LLM 客户端 / 对话压缩 / 长期记忆 / RAG / Token 预算 / Prompt Cache）
- 2 个模块只有设计（HITL / Skill）
- 约 2500 行 Java 代码
- 用 AI 辅助 vibe coding 完全可信

---

## 六、项目实际实现

### 创建项目 `~/Desktop/my-agent/`

从零搭建了完整的精简版 Agent 项目，24 个 Java 源文件，约 2600 行代码。

### 项目结构

```
~/Desktop/my-agent/
├── pom.xml
├── .env
├── CLAUDE.md
├── AGENTS.md
├── PAI.md
└── src/main/java/com/myagent/
    ├── cli/Main.java                    # 入口 + 启动画面 + 命令处理
    ├── agent/
    │   ├── Agent.java                   # ReAct 主循环
    │   ├── PlanExecuteAgent.java        # Plan-and-Execute 模式
    │   └── AgentGuard.java              # 循环保护（停滞检测 + 轮数上限）
    ├── llm/
    │   ├── LlmClient.java               # 接口 + 消息模型
    │   ├── AbstractOpenAiCompatibleClient.java  # SSE 流式基类
    │   ├── DeepSeekClient.java          # DeepSeek 实现
    │   ├── GenericOpenAiClient.java     # 通用 OpenAI 兼容客户端
    │   └── LlmClientFactory.java        # 工厂模式 + 热切换
    ├── tool/
    │   ├── ToolRegistry.java            # 工具注册 + 并行执行
    │   ├── ToolOutput.java              # 工具输出
    │   ├── PathGuard.java               # 路径安全
    │   ├── CommandGuard.java            # 命令黑名单
    │   └── PolicyException.java         # 安全策略异常
    ├── memory/
    │   ├── MemoryManager.java           # 记忆管理器
    │   ├── ConversationMemory.java      # 短期记忆
    │   ├── LongTermMemory.java          # 长期记忆（project/global）
    │   ├── ConversationHistoryCompactor.java  # 对话压缩
    │   ├── MemoryEntry.java             # 记忆条目
    │   └── TokenBudget.java             # Token 预算
    └── rag/
        ├── CodeIndex.java               # RAG 检索引擎
        ├── CodeChunk.java               # 代码块模型
        ├── CodeChunker.java             # 三级切块（JavaParser AST）
        ├── VectorStore.java             # SQLite 向量存储
        ├── EmbeddingClient.java         # Embedding API
        └── TfidfIndex.java              # TF-IDF 倒排索引
```

### 编译修复记录

1. **lambda 类型推断失败** — `args -> grepCode(args)` 加显式转型 `(ToolExecutor) args -> grepCode(args)`
2. **IOException 未捕获** — read_file / write_file 的 lambda 体加 try-catch
3. **jieba API 不匹配** — `JiebaSegmenter.SegmentMode.INDEX` 改为 `sentenceProcess()`，返回 `List<String>` 而非 `List<SegToken>`

### 项目配置文件

生成了三个 AI 协作文件：
- **CLAUDE.md** — 给 Claude Code/Cursor 的编码规范（~30 行）
- **AGENTS.md** — 给所有 AI 工具的完整项目手册（~170 行）
- **PAI.md** — 项目级记忆文件（~25 行）

---

## 七、终端 UI 美化

将简陋的 `println` 启动画面升级为 Claude Code 风格：
- ASCII 艺术 Banner（大号彩色 `MY AGENT` logo）
- 蓝底白字模型标签
- 绿色圆点工具状态
- 树形结构使用提示
- `❯` 青蓝色加粗提示符
- 思考指示器（`● 思考中...`）
- 分隔线 + ANSI 彩色输出

---

## 八、全局命令 `myagent`

创建了 `~/bin/myagent` 启动脚本，加入 `~/.zshrc` PATH。
任意目录下输入 `myagent` 即可启动 Agent。

---

## 九、模型热切换

新增功能：
- `LlmClientFactory.java` — 工厂模式，按 provider 名创建客户端
- `GenericOpenAiClient.java` — 通用 OpenAI 兼容客户端
- `/model` 命令 — 查看当前模型 / 切换到 deepseek/glm/kimi/qwen
- 对话历史切换后保留

`.env` 配置多 provider 示例：
```
DEEPSEEK_API_KEY=sk-xxx
GLM_API_KEY=xxx
KIMI_API_KEY=xxx
QWEN_API_KEY=xxx
```

---

## 十、Embedding 配置

RAG 向量检索需要配置 Embedding API：

千问（免费）：
```
EMBEDDING_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/embeddings
EMBEDDING_API_KEY=千问key
EMBEDDING_MODEL=text-embedding-v3
```

智谱 GLM：
```
EMBEDDING_API_URL=https://open.bigmodel.cn/api/paas/v4/embeddings
EMBEDDING_API_KEY=智谱key
EMBEDDING_MODEL=embedding-3
```

不配也能用，只走 TF-IDF 关键词检索。

---

## 十一、跨机器部署

当前项目只在本机可用。换台电脑需要：
1. 装 Java 17 + Maven
2. 拉代码 + 编译
3. 配 .env
4. 安装 myagent 命令

建议推到 Git 仓库，后续可以一键部署。
